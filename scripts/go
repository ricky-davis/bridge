#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
LOCAL_GO="${ROOT_DIR}/.tools/go/bin/go"

if [[ -x "${LOCAL_GO}" ]]; then
  exec "${LOCAL_GO}" "$@"
fi

exec go "$@"
